# fizzbuzz with Gareth, using a switch to conditionally select value in each index

+1 ⇡100
.
=0 [∩◿(5,3)]
⊢ ⟜⊣ # split the rows of the array into multiple-of-3 and multiple-of-5 mask
×2   # multiply multiple-of-5 mask by 2
+    # add to get selectors
⨬({∘}|{"Fizz"}|{"Buzz"}|{"FizzBuzz"})
♭
